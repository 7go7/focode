<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FOCODE - Contact</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        /* --- CORE VARIABLES --- */
        :root { 
            --primary: #000000;
            --accent: #800020; /* FOCODE Red */
            --text-dark: #111111;
            --bg-light: #fcfcfc;
            --ease-out: cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Segoe UI', Roboto, sans-serif; }
        body { background-color: var(--bg-light); color: var(--text-dark); line-height: 1.6; }
        a { text-decoration: none; color: inherit; transition: 0.3s; }

        /* --- 1. USER'S HEADER STYLES (INTEGRATED) --- */
        header { 
            background: white; 
            box-shadow: 0 1px 5px rgba(0,0,0,0.05); 
            position: sticky; top: 0; z-index: 1000; 
            border-bottom: 1px solid #eee;
        }

        .nav-container { 
            max-width: 1200px; margin: 0 auto; 
            display: flex; justify-content: space-between; align-items: center; 
            padding: 0 20px; 
            height: 70px; /* Standard Desktop Height */
            transition: height 0.3s ease;
        }

        /* LOGO */
        .logo { 
            font-size: 26px; font-weight: 900; color: var(--primary); 
            letter-spacing: -1px; text-decoration: none; text-transform: uppercase; 
            white-space: nowrap;
        }
        .logo span { color: var(--accent); }

        .nav-right { display: flex; align-items: center; gap: 20px; }

        /* BUTTONS */
        .btn-donate { 
            background: var(--accent); color: white; 
            padding: 10px 24px; 
            font-weight: bold; border-radius: 4px; 
            font-size: 13px; text-transform: uppercase; 
            border: none; cursor: pointer; white-space: nowrap;
        }
        .btn-icon {
            background: transparent; border: 1px solid #e0e0e0; color: #555;
            width: 40px; height: 40px; border-radius: 4px; 
            display: flex; align-items: center; justify-content: center; 
            font-size: 18px; cursor: pointer; transition: 0.2s;
        }
        .btn-icon:hover { background: #f9f9f9; color: var(--primary); border-color: #ccc; }

        /* DROPDOWN */
        .menu-wrapper { position: relative; }
        .dropdown {
            position: absolute; top: 50px; right: -10px;
            background: white; width: 220px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.15);
            border-radius: 8px; border: 1px solid #f0f0f0;
            opacity: 0; visibility: hidden; transform: translateY(10px);
            transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94); overflow: hidden;
            z-index: 1001;
        }
        .dropdown.active { opacity: 1; visibility: visible; transform: translateY(0); }
        .dropdown a { 
            display: flex; align-items: center; gap: 10px;
            padding: 14px 20px; border-bottom: 1px solid #f9f9f9; 
            font-size: 14px; color: #333; font-weight: 600;
        }
        .dropdown a:active, .dropdown a:hover { background: #f9f9f9; color: var(--primary); }
        .dropdown a i { width: 20px; text-align: center; color: #999; }

        /* MOBILE OPTIMIZATIONS */
        @media (max-width: 768px) {
            .nav-container { height: 55px; padding: 0 15px; }
            .logo { font-size: 20px; }
            .nav-right { gap: 10px; }
            .btn-donate { padding: 6px 12px; font-size: 11px; border-radius: 3px; }
            .btn-icon { width: 32px; height: 32px; font-size: 14px; }
            .dropdown { top: 45px; width: 200px; }
        }

        /* --- 2. PAGE CONTENT STYLES --- */
        .page-header-hero {
            background: #111; color: white; padding: 80px 0 60px; text-align: center;
            margin-bottom: 60px;
        }
        .hero-title { font-size: 42px; font-weight: 800; margin-bottom: 10px; letter-spacing: -1px; }
        .hero-subtitle { color: #aaa; font-size: 18px; max-width: 600px; margin: 0 auto; }

        .container { max-width: 1200px; margin: 0 auto; padding: 0 20px; }
        .contact-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 60px; margin-bottom: 80px; }

        /* LEFT COL */
        .info-card { background: white; padding: 40px; border-radius: 4px; box-shadow: 0 4px 15px rgba(0,0,0,0.05); margin-bottom: 30px; }
        .info-title { font-size: 22px; font-weight: 700; margin-bottom: 20px; border-left: 4px solid var(--accent); padding-left: 15px; }
        .contact-row { display: flex; gap: 15px; margin-bottom: 20px; }
        .c-icon { width: 40px; height: 40px; background: #f4f4f4; color: var(--accent); display: flex; align-items: center; justify-content: center; border-radius: 50%; font-size: 16px; flex-shrink: 0; }
        
        /* SECURE VAULT */
        .secure-box { background: #1a1a1a; color: white; padding: 35px; border-radius: 4px; position: relative; overflow: hidden; border-left: 4px solid var(--accent); }
        .secure-btn { display: inline-block; background: var(--accent); color: white; padding: 12px 20px; font-weight: bold; border-radius: 3px; margin-top: 15px; font-size: 14px; }
        .secure-btn:hover { background: white; color: var(--accent); }

        /* FORM */
        .form-group { margin-bottom: 15px; }
        .form-control { width: 100%; padding: 12px; border: 1px solid #ddd; background: #fafafa; border-radius: 3px; outline: none; transition: 0.3s; }
        .form-control:focus { border-color: var(--accent); background: white; }
        .submit-btn { background: #000; color: white; border: none; padding: 15px 30px; font-weight: bold; cursor: pointer; text-transform: uppercase; width: 100%; }
        .submit-btn:hover { background: var(--accent); }

        /* RIGHT COL (SOCIALS) */
        .social-grid { display: grid; grid-template-columns: 1fr; gap: 15px; }
        .social-card { display: flex; align-items: center; padding: 15px; background: white; border: 1px solid #eee; border-radius: 4px; transition: 0.3s; }
        .social-card:hover { transform: translateX(5px); border-color: var(--accent); box-shadow: 0 5px 15px rgba(0,0,0,0.05); }
        .s-icon { width: 40px; height: 40px; display: flex; align-items: center; justify-content: center; color: white; border-radius: 4px; margin-right: 15px; flex-shrink: 0; }
        
        /* BRAND COLORS */
        .bg-x { background: #000; }
        .bg-fb { background: #1877F2; }
        .bg-yt { background: #FF0000; }
        .bg-tt { background: #000; }
        .bg-ig { background: linear-gradient(45deg, #f09433, #dc2743, #bc1888); }

        /* ANIMATION */
        .reveal { opacity: 0; transform: translateY(20px); animation: fadeUp 0.8s forwards; }
        @keyframes fadeUp { to { opacity: 1; transform: translateY(0); } }

        @media (max-width: 768px) {
            .contact-grid { grid-template-columns: 1fr; gap: 40px; }
            .hero-title { font-size: 32px; }
        }
    </style>
</head>
<body>

    <header>
        <div class="nav-container">
            
            <div class="nav-left">
                <a href="javascript:history.back()" class="btn-icon" title="Retour">
                    <i class="fas fa-arrow-left"></i>
                </a>
            </div>

            <div style="position: absolute; left: 50%; transform: translateX(-50%);">
                <a href="/" class="logo">FOCODE<span>.</span></a>
            </div>

            <div class="nav-right">
                <a href="/donate" class="btn-donate">Donner</a>
                
                <div class="menu-wrapper">
                    <button class="btn-icon" onclick="toggleMenu()">
                        <i class="fas fa-bars"></i>
                    </button>
                    
                    <div class="dropdown" id="mainMenu">
                        <a href="/"><i class="fas fa-home"></i> Accueil</a>
                        <a href="/about"><i class="fas fa-users"></i> Qui sommes-nous</a>
                        <a href="/secure"><i class="fas fa-user-shield"></i> Signalement</a>
                        <a href="/donate" style="color:var(--accent)"><i class="fas fa-heart"></i> Faire un don</a>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <section class="page-header-hero">
        <div class="container reveal">
            <h1 class="hero-title">Contact & Communauté</h1>
            <p class="hero-subtitle">Rejoignez notre écosystème numérique ou contactez-nous en toute sécurité.</p>
        </div>
    </section>

    <div class="container">
        <div class="contact-grid">
            
            <div class="left-col">
                
                <div class="secure-box reveal" style="animation-delay: 0.1s;">
                    <h3 style="font-size:20px; margin-bottom:10px;"><i class="fas fa-user-shield"></i> Lançeurs d'Alerte</h3>
                    <p style="color:#aaa; font-size:14px; margin-bottom:20px;">
                        Pour votre sécurité, n'utilisez pas ce formulaire standard pour des documents sensibles.
                    </p>
                    <a href="/secure-submission" class="secure-btn">PORTAIL SÉCURISÉ (TOR)</a>
                </div>

                <br>

                <div class="info-card reveal" style="animation-delay: 0.2s;">
                    <h3 class="info-title">Formulaire Général</h3>
                    <form action="/contact" method="POST">
                        <div class="form-group">
                            <input type="text" name="name" class="form-control" placeholder="Nom" required>
                        </div>
                        <div class="form-group">
                            <input type="email" name="email" class="form-control" placeholder="Email" required>
                        </div>
                        <div class="form-group">
                            <textarea name="message" rows="4" class="form-control" placeholder="Message..." required></textarea>
                        </div>
                        <button type="submit" class="submit-btn">Envoyer</button>
                    </form>
                    
                    <div style="margin-top: 30px; border-top: 1px solid #eee; padding-top: 20px;">
                        <div class="contact-row">
                            <div class="c-icon"><i class="fas fa-envelope"></i></div>
                            <div>
                                <b>Email Presse</b><br>
                                <a href="mailto:info@focode.org" style="color:#666; font-size:14px;">info@focode.org</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="right-col reveal" style="animation-delay: 0.3s;">
                <h3 style="font-size: 24px; font-weight:800; margin-bottom: 25px;">Notre Écosystème</h3>
                
                <div class="social-grid">
                    <a href="https://x.com/FOCODE_/status/2002062413189754973?s=20" target="_blank" class="social-card">
                        <div class="s-icon bg-x"><i class="fa-brands fa-x-twitter"></i></div>
                        <div>
                            <h4 style="font-size:16px; font-weight:700;">X (Twitter)</h4>
                            <span style="font-size:12px; color:#777;">Alertes en temps réel</span>
                        </div>
                    </a>

                    <a href="https://www.youtube.com/@focodeasbl" target="_blank" class="social-card">
                        <div class="s-icon bg-yt"><i class="fab fa-youtube"></i></div>
                        <div>
                            <h4 style="font-size:16px; font-weight:700;">YouTube</h4>
                            <span style="font-size:12px; color:#777;">Enquêtes & Analyses</span>
                        </div>
                    </a>

                    <a href="https://www.facebook.com/FocodeAsblBurundi/" target="_blank" class="social-card">
                        <div class="s-icon bg-fb"><i class="fab fa-facebook-f"></i></div>
                        <div>
                            <h4 style="font-size:16px; font-weight:700;">Facebook</h4>
                            <span style="font-size:12px; color:#777;">Communauté</span>
                        </div>
                    </a>

                    <a href="https://www.instagram.com/focodeasbl/" target="_blank" class="social-card">
                        <div class="s-icon bg-ig"><i class="fab fa-instagram"></i></div>
                        <div>
                            <h4 style="font-size:16px; font-weight:700;">Instagram</h4>
                            <span style="font-size:12px; color:#777;">Campagnes visuelles</span>
                        </div>
                    </a>
                    
                    <a href="https://www.tiktok.com/@focodeasbl" target="_blank" class="social-card">
                        <div class="s-icon bg-tt"><i class="fab fa-tiktok"></i></div>
                        <div>
                            <h4 style="font-size:16px; font-weight:700;">TikTok</h4>
                            <span style="font-size:12px; color:#777;">Format court</span>
                        </div>
                    </a>
                </div>

                <div style="margin-top: 30px; background: #eee; height: 200px; border-radius: 4px; display: flex; align-items: center; justify-content: center; color: #999;">
                    <i class="fas fa-map-marker-alt" style="margin-right: 10px;"></i> Basé en Exil
                </div>
            </div>

        </div>
    </div>

    <footer style="background: #111; color: white; padding: 60px 20px; text-align: center; margin-top: 60px;">
        <div class="container">
            <p style="color:#777; font-size:14px;">© 2025 FOCODE. Tous droits réservés.</p>
        </div>
    </footer>

    <script>
        function toggleMenu() {
            const menu = document.getElementById('mainMenu');
            menu.classList.toggle('active');
        }

        document.addEventListener('click', function(event) {
            const menu = document.getElementById('mainMenu');
            const btns = document.querySelectorAll('.btn-icon');
            let clickedBtn = false;
            btns.forEach(btn => { if(btn.contains(event.target)) clickedBtn = true; });
            
            if (menu && !menu.contains(event.target) && !clickedBtn) {
                menu.classList.remove('active');
            }
        });
    </script>
</body>
</html>